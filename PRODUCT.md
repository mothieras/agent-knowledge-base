# PRODUCT — Agent Shared Context

> 版本：v1.3 阶段性总纲，2026-09-18。D2–D5 设计决策已收敛并随阶段 2 T1–T5 交付（交付证据以 [README](README.md) 为准）；正文与交付状态已同步。v1.1（2026-09-14）补充与原生记忆互补的定位及价值验证方向。用于指导后续设计与实施，已确认方向、实施建议和待决策项分别标注。Agent Shared Context 是工作名称，暂不更名仓库。
> 本文定义产品目标与行为边界；[ROADMAP](ROADMAP.md) 定义建设顺序与验收条件；[README](README.md) 记录已实现能力与验证证据；[ARCHITECTURE](docs/ARCHITECTURE.md) 约束现有分层。

“已确认”是后续工作的产品约束；“工作定义/建议”是设计起点，实施前结合用例细化；“待决策”按第 8 节规定的阶段处理。文档定稿不表示建议已实现，也不要求先解决全部待决策项才能开始实施。本文中的第一版指单用户、多 Agent 共享服务的初始范围；阶段 1 的文档入库优化已独立交付。

## 1. 产品定位

给不同 Agent 共同使用的自托管持久知识与事实服务：保存、检索和修订 Memory 与 Knowledge，连接原始资料、积累的条目及其修订历史，让一个 Agent 记录的信息，可以被自己在后续任务中、或其他接入的 Agent 继续使用。

本服务与 Codex、Claude Code 等调用方的原生记忆配合使用，重点补足以下流程：

- **跨 Agent 共享**：不同工具或运行环境中的 Agent 通过同一服务读写内容，保留统一的条目身份与适用范围。
- **资料与条目关联**：导入的资料可直接检索；调用方整理出的事实、方法或结论，可以回查到作为依据的原文修订与片段。
- **共享内容持续修订**：信息变化后更新同一条目，保留历史；并发修改、重试和到期处理遵循明确规则。

与原生记忆互补是已确认的定位，不以替代其偏好记录、会话总结或自动回忆为目标。调用方通过公开工具保存和取用内容，无需服务接管或同步各工具的内部记忆文件；原生记忆也不是使用本服务的前置条件。上述流程是产品目标，不构成对特定厂商当前能力或未来边界的断言。

这里的 Shared Context 首先指可持久保存、可共享的信息。任务调度、实时进度同步、聊天会话恢复不因为使用这个名称而自动纳入范围。

核心使用过程：Agent 获得信息或收到用户的记录指令 → 写入条目 → 后续任务检索并回查 → 发现变化后修订。

## 2. 已确认的产品决策

| 决策 | 约定 |
|---|---|
| 内容分类 | 明确区分 Memory 与 Knowledge；不按保存时间长短分类 |
| 自主写入 | 调用方 Agent 可以自行判断并记录，无需用户逐条确认 |
| 模型依赖 | 核心服务在没有模型的情况下也应能正常运行；语义判断由调用方 Agent 承担 |
| 用户范围 | 第一版单用户，不做多用户或租户隔离 |
| 共享范围 | 第一版所有获准接入的 Agent 都可以读取和修改全部记录，不做 Agent 私有分区 |
| 内容关联范围 | 支持全局、关联单个指定项目、关联多个指定项目；与 Memory/Knowledge 类型独立 |
| 搜索范围 | 指定项目默认包含全局；多项目取并集并去重；未指定范围只查全局；跨所有项目须显式指定 |
| 保留与到期 | 两类内容默认持久保留，不因久未访问自动遗忘；调用方可指定有效期，到期退出默认检索但保留原文与历史 |
| 生命周期操作 | 归档、删除与恢复由调用方或用户明确触发；服务不自行判断内容应被遗忘 |
| 文档处理定位 | 作为值得投入的基础能力，提升解析、分块和证据质量；产品重心仍是 Memory/Knowledge 的记录、共享与复用 |
| 建设顺序 | 先小步优化已有文档入库流程，再建设调用方 Agent 的自主决策、记录与共享流程 |
| 协作修改 | Agent 可以修订其他 Agent 创建的条目，但必须处理并发，不能静默丢失更新 |

Agent 标识用于记录作者和修改者，不作为第一版权限隔离边界。访问服务的凭据与记录作者身份是不同概念；D2 已收敛：author 为必填自报字符串，与部署级单 Bearer 凭据不绑定，不能把客户端自报身份当作强身份保证。

## 3. 内容模型

Memory 与 Knowledge 是本产品采用的内容分类，不是对“记忆”和“知识”的普遍定义。必须区分两类的方向已经确认，以下给出指导后续工作的定义与样例；混合内容和模糊情况的处理规则已随条目契约收敛（分类边界样例见[工具说明](docs/ENTRY_TOOL_GUIDE.md) §2），不能仅靠中英文名称判断类型。

### Memory

关于具体用户、项目、环境或工作经历的事实、偏好、约定与决策。即使长期有效，仍属于 Memory。

- “这个项目使用 Java 21。”
- “用户要求提交前先运行测试。”
- “上次服务启动失败，是因为评测进程占用了索引目录。”

### Knowledge

可学习、可复用的方法、概念和解释。知识可以有适用条件和版本，不保证永久正确，也不要求适用于所有项目。

- “Python 生成器通过 yield 暂停和恢复执行。”
- “Spring Boot 中某种事务传播行为的用法及限制。”

类型由调用方明确指定，服务只校验枚举、不依赖模型自动分类。混合内容由调用方自行拆分为多条（服务不拆不拒）；类型修正 = 正常修订（条目 ID 不变、不含类型信息）。

### 分类依据（工作定义）

Memory 主要回答“某个主体实际是什么状态、经历过什么、偏好什么、约定或决定了什么”；Knowledge 主要回答“某个概念是什么、为什么如此、在什么条件下如何完成某件事”。按记录的主要用途分类，不按作者、长度、保存时间、可信度或关联项目数量分类。

| 内容 | 建议类型 | 理由 |
|---|---|---|
| “项目 A 选择 Python 3.12。” | Memory | 保存该项目实际采用的配置或决策 |
| “Python 生成器的用法与适用场景。” | Knowledge | 保存可供理解和应用的方法 |
| “用户希望所有项目默认使用中文回复。” | Memory | 保存用户偏好；可以是全局记录 |
| “项目 A 的发布流程、各步骤的原因与排障方法。” | Knowledge | 保存操作知识；可以只关联项目 A |
| “上次发布失败，因为评测进程占用索引目录。” | Memory | 一次经历的事实 |
| “从那次排障总结出：重建索引前必须停服务。” | Knowledge | 从经历提炼的可复用方法 |
| “embedded Qdrant 同目录只能一个进程持有。” | Knowledge | 环境约束的通用化表述；若只记“这台机器当前的状态”则是 Memory |
| “项目 B 用 Rust，因为延迟要求 <5ms。” | 拆两条 | 决策事实（Memory）与选型理由/适用条件（Knowledge）分开记 |

两类都可复用，也都可能过时或出错。项目相关内容不必全是 Memory，全局内容也不必全是 Knowledge。一次具体排障经历可以作为 Memory 保存，从中整理出的排障方法可以另记为 Knowledge，并关联原始记录；不因复用次数增加就自动转换类型。

建议两类共用保存、版本、来源与并发契约，类型用于表达内容用途和支持检索筛选。Memory 标签不自动赋予内容指令优先级，Knowledge 标签不代表已核实；是否需要类型特有的行为，须有具体场景后再定。

### 内容关联范围

已确认支持全局、单个指定项目和多个指定项目。两类条目均支持这三种范围；关联范围是组织与检索维度，不是权限边界，也不证明内容在该范围内无条件正确。

建议采用明确的全局范围，或一个包含至少一个项目的指定项目集合。多项目关联指同一条目关联多个项目，不需要复制正文；修改条目会影响所有关联项目，修改关联关系也应接受版本检查。

全局条目不需要逐个关联项目，指定新项目搜索时同样纳入全局内容。不能把“全部现有项目”的枚举与“全局”静默等同。

### 搜索语义（已确认）

提供统一搜索，Memory/Knowledge 类型与项目范围作为独立筛选条件；默认同时检索两种类型，调用方可以明确只查其中一种。

| 调用方指定的范围 | 候选内容范围 |
|---|---|
| 未指定范围 | 仅全局内容 |
| 明确只查全局 | 仅全局内容 |
| 项目 A | 全局内容，加上关联 A 的内容 |
| 项目 A、B | 全局内容，加上关联 A 或 B 的内容；按条目 ID 去重 |
| 明确跨所有项目 | 全局内容和所有项目关联内容 |

项目集合按“关联任意一个指定项目”匹配，不要求条目同时关联全部指定项目。范围过滤限定参与检索的候选内容，实际返回仍受相关性、数量及内容预算限制；“包含全局”不表示每次必须返回全部全局条目。

第一版范围筛选不构成访问控制，调用方可以明确扩大搜索范围。搜索结果应提供内容类型、关联范围、来源、修订与更新时间，以及按最终生命周期契约定义的有效性信息。

全局与项目内容可以同时返回。服务不因为内容属于项目范围，就自动覆盖或隐藏全局内容，也不自动判断二者语义冲突；调用方结合范围与来源判断如何使用。

### 保留、到期与遗忘（已确认）

Memory 与 Knowledge 采用相同的基础保留原则：默认持久保留，不因为记录时间久、访问次数少或长期未访问而自动遗忘，也不根据内容类型推断默认寿命。

调用方可以在写入或修订时明确指定有效期。服务到期后按规则将条目移出默认检索，不要求到期时再次调用，也无需模型判断；原文和修订历史继续保留，供显式历史查询与回查使用。到期不等于删除，也不表示历史事实变成错误。

| 情况 | 处理规则 |
|---|---|
| 未设置有效期，长期未访问 | 不因时间或访问频率改变保留状态与默认检索资格 |
| 达到调用方指定的有效期 | 自动退出默认检索，保留原文、历史和到期信息 |
| 调用方或用户明确归档、删除、恢复 | 按对应操作契约执行，服务不自行触发 |

项目范围和类型筛选不会自动放宽有效期过滤；显式跨所有项目搜索也默认排除已到期条目。回查已到期内容时保留到期信息，不能将历史内容呈现为当前有效。

有效期的修改沿用条目修订和并发检查。延长、清除有效期或恢复条目均走生命周期/修订操作并产生修订；访问不会自动续期，旧有效期的处理不会覆盖已经提交的新修订（到期在查询时判定，设计上无后台改写）。

已确认的是保留原则与操作触发方式。D4 已收敛并交付：三态 `active / archived / deleted` 全软转换（archive/unarchive/delete/restore 四操作，均携带版本检查并产生修订），无永久清除；正文与修订历史全保留，删除后历史保留期限显式推迟至 D7。“到期仍保留历史”不解释为显式删除后也必须永久保留内容。

### 条目与来源文档

Memory 与 Knowledge 的日常记录以条目为中心。条目可以是一句话，也可以是一段完整解释；不要求一句话一条，也不限制 Agent 写长内容或用户直接添加短记录。

导入文档保留原文，不要求导入时自动提炼成 Memory 或 Knowledge。调用方 Agent 可以从文档提取条目，并保留指向原文修订及片段的来源关系。

一份文档可能同时承载 Memory 和 Knowledge。检索 chunk 是技术分块，不因此成为独立条目。来源存在只表示可追溯，不表示内容已验证正确。

## 4. 核心场景

### 自主积累 Memory

Agent 在任务中发现一条值得后续复用的事实或决策，写入 Memory，并说明适用范围与依据。后续由同一 Agent 或不同工具中的 Agent 检索使用，无需用户重新转述，也不要求它们共享原生记忆目录。

### 用户指定保存 Knowledge

用户要求保存一种技术方法或研究资料中的解释。Agent 整理成 Knowledge，保留适用条件和资料来源，按需要关联一个或多个项目；后续相关任务中的 Agent 可以检索复用。编程用法是示例，内容范围不限于代码或排障经验。

### 跨 Agent 复用与修订

Agent A 记录项目约定，使用另一客户端的 Agent B 在后续任务中读取。B 发现约定已变化，可以修订同一条记录；A 的旧版本修改不能覆盖 B 已提交的新版本。后续读取可取得当前修订，历史回查仍能说明当时记录了什么。

### 从文档积累条目

用户导入文档后可以直接检索原文。Agent A 根据需要提取条目，Agent B 在其他任务中命中条目后，按预算回查作为依据的原文片段。文档更新后，旧条目的来源仍指向提取时的文档版本；来源变化后的复核规则按 D6 确定。

以上场景中，自主写入、协作修订与历史回查能力已随阶段 2 T1–T5 交付（证据见 README）；「从文档积累条目」的派生条目与来源关系（D6）尚未交付。真实任务中的使用收益仍待 T6 验证。

## 5. 服务与调用方的职责

| 调用方 Agent | 服务 |
|---|---|
| 判断是否值得保存，整理正文，指定内容类型 | 校验字段、持久保存、返回稳定标识 |
| 提供来源、适用范围及观察或推断性质 | 保留元数据与来源关系，不替内容背书 |
| 判断需要新增还是修订哪条记录 | 执行明确操作，管理修订和并发 |
| 读取冲突后的内容，决定如何整合 | 返回冲突及当前版本，不自动语义合并 |
| 根据任务检索并选择使用哪些内容 | 提供检索、过滤、原文和版本回查 |

调用方自主记录以用户授予的工具权限为前提；自主记录能力不要求服务自行读取全部聊天或工作日志。

调用方负责在原生记忆与本服务之间选择适当的记录和取用方式。建议在本地摘要中保留共享条目的 ID，需要判断当前状态时回查服务；服务只能保证自身接口的修订与有效性语义，不能自动刷新已经进入调用方上下文或原生记忆的副本。原生记忆适配和自动双向同步不属于当前交付范围。

## 6. 建议采用的基础契约

本节是支撑已确认方向的基础契约。字段、错误格式与保留策略已随阶段 2 spec（PHASE2）冻结并交付，本节保留产品层面的契约要点；实现细节以 README 与[工具说明](docs/ENTRY_TOOL_GUIDE.md)为准。

### 记录与修订

条目应包含稳定 ID、类型、正文、适用范围、作者、时间、修订号和生命周期状态；来源信息按实际可得情况记录。不能提供外部来源时允许明确声明来源未知，不伪造引用。

项目、主题等组织信息只用于筛选，不构成第一版权限边界。原始声明、观察与推断应能区分，具体字段枚举待定。

### 并发与重试

- 修改携带预期修订号；版本比较、内容更新与修订历史提交必须原子完成。
- 两个 Agent 基于同一旧版本修改时，只允许一个成功；另一个收到冲突，重新读取后再判断，不能盲目重试覆盖。
- 写入支持幂等标识，处理超时后的重复提交；相同标识、不同请求内容应返回错误。D5 已收敛：幂等键全局唯一（跨条目、跨作者）、记录持久保留无 TTL，同键同内容重放原结果、同键异内容显式冲突。
- 修改、归档、失效和删除类操作采用同样的并发纪律（生命周期操作集已定：archive/unarchive/delete/restore，均携带版本检查并产生修订）。
- 分别新增的语义重复条目可以暂时共存；服务不依赖模型自动去重或合并。

### 保存与搜索一致性

已交付：写入成功后按 ID 立即可读最新修订，写入与 FTS 索引更新同事务提交——无异步索引状态，不把未完成的索引更新报告为搜索已就绪。

搜索命中应绑定实际返回的内容修订，回查不能偷偷返回同 ID 的其他版本。索引可重建，已保存的正文、修订和来源不能只存在于搜索索引中。

### 无模型运行

“不需要生成模型”与“完全不需要模型”不同：当前语料检索仍使用 embedding 模型。

已交付无需 embedding 或生成模型的条目全文检索路径（SQLite FTS5 + jieba 空格预分词 + bm25），写入、回查、修订和搜索可以独立运行，禁全部模型下核心流程经实测可用；向量检索、rerank 与已有问答功能作为可选能力。条目检索挑战集独立冻结并达标，当前 README 的语料检索指标不直接沿用为条目路径指标。

### 文档处理与分块（投入方向已确认，细节为建议）

文档是长期内容的重要来源，解析与分块应帮助 Agent 获得完整、可追溯的证据。投入优先围绕正文质量、结构保留、来源定位与检索效果开展，具体实现深度由代表性资料和评测结果决定。

已有离线入库代码接收 Markdown、UTF-8 TXT 和 PDF，统一转为 Markdown 后进入父子分块；这不代表已交付通用上传服务或完成所有格式的质量验收。阶段 1 已补齐：入库失败终态结构化、无文本层 PDF 显式失败（`no_text_layer`）、PDF 分支严格禁用 OCR 的行为双证据（依赖门控：装入 OCR 依赖后图像页会自动启用，升级依赖须重验）、原文件定位显式 missing。复用这些能力时，也须保留 README 中当前评测的适用范围。

建议采用以下质量目标：

- **解析与规范化**：在选定的常见格式上保留正文顺序、标题层级、列表、代码块和表格关系；空文本、解析失败或已知丢失内容应明确报告。
- **按结构分块**：以章节、段落和内容结构为边界，尽量保留代码块、表格与解释的关系；短 Memory/Knowledge 可完整参与检索，长条目与文档可产生多个检索块，但保持同一内容身份。
- **上下文回查**：命中较小片段后，可按预算取得所属章节或相邻上下文；避免为提高召回而无界返回整份长文档。
- **证据定位**：保留原始文件修订、规范化文本和解析版本；字符 span 与内容 hash 明确绑定实际检索文本。页码等原文件定位信息单独记录，无法恢复时明确缺失，不把规范化文本偏移冒充原文件偏移。
- **质量评测**：用包含标题、代码、表格及复杂阅读顺序的代表性文档，分别检查内容保留、分块完整性、证据定位与最终检索效果。

非 OCR 解析路径避免引入必需模型推理。首步优化以已有 Markdown、TXT、文本型 PDF 为起点，DOCX、HTML 等常见格式按实际需求扩展，不将完整格式清单作为这次小步优化的门槛。现有 embedding 检索仍有模型依赖，完整无模型搜索在后续条目服务阶段补齐。OCR、图像理解及复杂版式的完整还原不自动纳入。

## 7. 分步交付与范围

建设顺序已确认，详细阶段见 ROADMAP：

1. **先优化现有文档入库流程**：沿已有离线入口改进解析、分块、失败处理与证据回查，用现有和新增样例证明改进；可以独立交付，不等待条目写入与共享机制。
2. **再建设 Agent 自主记录与共享**：提供条目写入、读取、搜索、协作修订和历史追溯，经 HTTP/MCP 供外部 Agent 使用；调用方负责自主决策，服务负责可靠执行。随后连接文档提取条目与来源修订流程。

第一版不做多用户、Agent 私有分区和细粒度授权。自动记忆提取、语义合并、按访问频率或服务端语义判断自动遗忘、自动学习排序、实时任务协同、会话恢复暂不纳入草案范围；调用方指定有效期后的确定性到期处理属于已确认范围。如需其他自动整理能力，再单独讨论。

文档处理与分块是先行投入的基础能力，具体优化项与新增格式在阶段开始时按证据确定。通用在线上传、异步入库平台与完整文档历史不自动并入首步。现有双模式问答是已实现的可选功能，不作为核心服务正常运行的必要条件；是否继续扩展另行判断。

## 8. 成功标准与待决策项

目标验收应证明：跨任务、跨 Agent 能取回已保存的信息；服务重启后记录仍在；并发修改不丢更新；超时重试不重复写入；旧证据不会指向新内容；关闭全部模型后核心流程仍可完成。前六项已由阶段 2 T1–T5 实测达标（证据见 README「条目服务」节）；跨 Agent 的真实任务收益仍待 T6 验证，检索效果另用真实场景构造评测，不仅以接口成功作为完成标准。

产品价值须围绕第 1 节的三个流程验证：在保留调用方原生记忆能力的情况下，接入服务是否减少跨工具重复转述、资料重新查找或错误使用旧内容。阶段 2 需覆盖不同 Agent 客户端的实际任务，文档关联能力在对应子步骤交付后验证。建议比较“原生记忆”与“原生记忆 + 本服务”，具体样例、指标与门槛在对应子步骤实施前冻结；接口正确性与任务收益分别报告，不把写入成功或检索召回率等同于产品收益。

以下事项保留给后续对应阶段，未定内容不视为已批准的默认行为：

| 编号 | 待确定内容 | 处理时机 |
|---|---|---|
| D1 | **已收敛**（2026-09-14，阶段 1 交付）：代表性样例集 `data/ingest_samples/`（S1–S10 + Level A/B 两级验证 harness）冻结并落地；管道质量门槛（逐样例显式终态、锚点保留、span/hash 可复算）以测试固化；优化靶点为入库失败终态结构化、无文本层 PDF 显式失败、原文件定位显式 missing、分块边界证据，全部交付（证据见 README「Ingest 样例集与失败终态」与 ROADMAP 阶段交付记录）；新增格式仍按需求选择 | 已完成 |
| D2 | **已收敛（设计，2026-09-16，阶段 2 spec）**：类型仅枚举校验，服务不分类、不判对错；混合内容由调用方自行拆分（服务不拆不拒），类型修正走正常修订；author 为必填自报字符串（≤128 字符，约定「客户端/版本」格式），与部署级单 Bearer 凭据不绑定、非强身份（工具说明明示）；字段约束冻结（body ≤64 KiB 等）。观察/推断性质区分仍留白，按真实场景证据再定 | 已交付（阶段 2 T1–T2） |
| D3 | **已收敛（设计，2026-09-16，阶段 2 spec）**：项目 = 规范化字符串标签（trim + 折叠小写 + `^[a-z0-9][a-z0-9_-]{0,63}$`，违法显式 400，不静默改写），无项目管理 API——不提供创建/重命名/删除，「项目删除」因无该操作而消解；条目关联 = 标签集合（1–16 个），「不存在的项目」不是错误 | 已交付（阶段 2 T1–T3） |
| D4 | **已收敛（设计，2026-09-16，阶段 2 spec）**：三态 active/archived/deleted + archive/unarchive/软删除/恢复四操作，全部携带版本检查并产生修订；正文与历史全保留；永久清除与删除后历史保留期限显式推迟至 D7；expires_at 为可选时间戳，查询时判定，到期退出默认搜索、显式回查保留到期信息、访问不续期 | 已交付（阶段 2 T4） |
| D5 | **已收敛（设计，2026-09-16，阶段 2 spec）**：存储 SQLite(WAL)+FTS5；无模型检索 = FTS5 + 空格预分词（jieba 经 T3 实测采定），挑战集/qrels/门槛在实现前冻结、独立报告；写入到搜索可见 = 同事务同步提交；幂等键全局唯一、同键异内容显式冲突、记录持久保留；容量/并发锚点 = 1 万条目 / 10 万修订 / 3 并发写 / 搜索 P95 < 500ms | 已交付（阶段 2 T3、T5 实测达标） |
| D6 | 来源更新后的派生条目复核规则，文档历史及在线/异步导入的范围 | 阶段 2 文档来源关联子步骤前 |
| D7 | 部署及备份恢复要求，真实资料的保留与恢复目标 | 承载唯一副本的真实资料前，最迟在阶段 3 对应工作前 |

不在本总纲中预先选择数据库、队列或全文索引引擎；技术设计应服务上述契约。后续确认待决策项时更新本文，阶段完成后将真实能力与证据写入 README；调整已确认的产品边界时同步 ROADMAP。
