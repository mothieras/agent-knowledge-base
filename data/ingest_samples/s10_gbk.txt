本文件是入库管道样例集的 S10（非 UTF-8 编码负样例）。
内容为合法 GBK 编码的中文文本，用于验证 TXT 读入路径对非 UTF-8 文件给出显式失败，
而不是抛出裸解码异常后被通用异常处理吞掉。GBK sentence anchor: 编码契约是入库契约的一部分。
本文件是入库管道样例集的 S10（非 UTF-8 编码负样例）。
内容为合法 GBK 编码的中文文本，用于验证 TXT 读入路径对非 UTF-8 文件给出显式失败，
而不是抛出裸解码异常后被通用异常处理吞掉。GBK sentence anchor: 编码契约是入库契约的一部分。
本文件是入库管道样例集的 S10（非 UTF-8 编码负样例）。
内容为合法 GBK 编码的中文文本，用于验证 TXT 读入路径对非 UTF-8 文件给出显式失败，
而不是抛出裸解码异常后被通用异常处理吞掉。GBK sentence anchor: 编码契约是入库契约的一部分。
