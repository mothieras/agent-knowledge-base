# S1 续（用户显式记录）

用户补充要求：把 mdstats 的 CLI 参数约定记到共享库——

> 「把 mdstats 的 CLI 参数约定（位置参数为文件路径、可选 --json、不引入子命令）
> 记到共享知识库，type 用 knowledge，同时关联 mdstats 和 cli-tools 两个项目
> （这个约定对其他 CLI 工具也适用），author 写 pi/0.86.0，
> 幂等键用 pi-s1-cli-contract。记完把条目 ID 报给我。」

（如果这条约定已经记过了，查一下已有条目，把 body 补全到这个粒度并用
revise_entry 修订，而不是重复新增。）
