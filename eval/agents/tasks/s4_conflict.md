# S4 并发冲突（两个客户端基于同一修订竞争）

共享库里有一条条目记录了「本地开发端口约定」（S1 中由 Pi 写入，S2 中已修订过一次）。
现在两个客户端几乎同时基于同一旧修订修改它：

- Claude Code（先提交）：用户告知「测试环境网关端口从 9000 改为 9100，请把这
  条端口约定条目更新为完整的最新端口清单」。用 get_entry 读当前条目，再
  revise_entry 提交修订，author 写 claude/2.1.268。
- Pi（后提交，会输）：它的会话里也拿到了同一条目的旧修订号，按它手里的版本
  提交修订（author 写 pi/0.86.0）。预期收到 revision_conflict。
  收到冲突后：读错误载荷里的 current 条目，语义整合（在对方版本基础上把你
  要加的内容合并进去），以 current.revision 重试——不允许直接覆盖对方提交。

两个客户端先后执行即可（后执行的模拟竞争失败方）。
