# 价值对照 任务2（臂通用）——扩展 --lines-only

你在数据平台团队的 `mdstats` 项目里工作。

1. 为 mdstats 增加 `--lines-only` 参数：只输出行数（文本模式输出 `N 行`，
   `--json` 模式输出 `{"file": ..., "lines": N}`），与既有参数可组合
2. 用 unittest 补一个用例覆盖它，运行 `python3 -m unittest` 全绿
3. 遵守项目约定

完成后报告：实现方式、测试结果。
