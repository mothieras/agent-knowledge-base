# 作战计划

> 单人项目的作战计划，一份文档，取代原 PRD / DESIGN / ROADMAP（原样归档于 [docs/archive/](docs/archive/)，仅作追溯）。
> 已完成能力与验证证据只以 [README](README.md) 为准；本文只回答三件事：最终做成什么、还差哪两步、什么纪律不能破。

## 定位

最终要完成的是一个**给 Agent 用的自托管知识库**：多 Agent 各自持身份，共享公共资料与私有分区，支持协作写入、异步入库和文档历史（细节见归档 PRD 的 F-01~F-09，另行排期）。当前交付物是它的第一块切片——**检索优先演示版**：受控语料、无生成模型也可用的检索与证据回查、单次双模式问答（`rag` 单图 / `agentic` 双图）、HTTP/MCP 接入、可复现评测。

使用场景（现在到将来）——过程上下文归调用方的 agent，事实依据归本库：

- **Agent 接地**（重心，已就绪）：外部 MCP 客户端（Pi 等）把 search/get_context 当工具，取带 span/版本的证据，用自己的模型回答
- **问答委托**（已交付）：应用调 ask_knowledge 得 decision（answered/澄清/拒答）与机械校验过的引用；rag/agentic 双模式同时是接地质量与编排成本的测量仪
- **多 Agent 共享知识**（发布后）：身份、公共/私有分区、协作写入、文档历史——知识库本体

memory 语义明确不做：服务只存取文本与元数据，不自动提取、合并或遗忘"记忆"，不靠反馈自动学习排序。"接手开源底座改造"的叙事与上游/增量对照见 README。

## 收尾两步

演示版的评测核心（同配置 rag/agentic 对照、40 题挑战集）已完成，剩两步，顺序执行：

### 第一步：对照实验定稿

- **重跑基线**：在最终 commit 重跑两份 mode 基线（当前绑定检索服务化 commit `745f82a`，与发布代码对不上）；报告与 README 基线表由同一场运行生成，消除现存的不一致（answered 计数、成本口径、泄漏计数）
- **badcase 审计**（轻量，写进对照报告，不设门禁）：rag 模式 5 个误拒逐条归因（单图一次检索证据不足即放弃，双图如何救回）；agentic 澄清计数口径与 per-item 分布的出入一并核对；歧义题 4/5 未澄清是否合理；挑战集 2 题 hard_negative 泄漏——其一 ch029 是"当前有效"问题命中过期版本 v1，留作版本过滤的靶子
- **规模与并发实测**：受控合成规模快照（约 100 文档 / 1 万块，不混入质量语料），3 并发执行 ≥100 个有效只读查询；记录 P50/P95、错误、排队与峰值资源；未完成或未达标如实标注
- 产出：一份对照报告（质量 / decision / 延迟 / 成本 / badcase 小节），作为发布证据的核心

### 第二步：发布

- Docker 原生流程一遍：镜像构建 → 启动 → health ready → HTTP/MCP smoke
- Pi 实测端到端：工具发现、Bearer、search/get_context、至少一次答案消费；脱敏记录版本与协议
- fresh clone 空数据复现：依赖安装 → 入库 → 启动 → smoke
- README 收尾：验证过的能力从目标挪到已完成；发布清单逐项列通过 / 失败 / 未执行

## 评测纪律

只有这几条是硬的，其余数字是记录、不是门禁：

1. 质量集完整执行，0 ERROR / 0 SKIP；错误与跳过保留在分母
2. 每份报告绑定 commit / index_id / 模型 / 参数；发布证据必须出自最终 commit
3. 饱和数字如实标注口径：Recall 1.000 只是这套固定语料 golden set 的结果，不是通用上限
4. 负结果有效：不预设双图更好；不改题、不删 badcase、不挑最好一次
5. 历史口径不冒充新口径：legacy 非误拒率 ≠ refusal precision，HITL 暂停率 ≠ 单次澄清率，并列不同比
6. 改检索 / Agent / 语料才重跑全量 eval；纯文档改动不跑付费评测

## 范围变更原则

进度不足时，最先停的是附加算法、界面美化与额外客户端适配；评测、证据正确性、清晰的错误行为最后停。

## 发布之后

1. **版本 / 有效期过滤**（第一优先）：fixture、qrels、泄漏靶子（ch029）已备，L0 执行过滤 + 泄漏回归，半天到一天。注意 version_conflict 类题按设计需返回两版，过滤不得误伤
2. **知识库形态**（另行排期，按归档 PRD F-01~F-09）：先设计权限过滤、搜索→回查一致性与发布状态机，再开写接口；不先开放写入再补授权
3. **检索实证升级**：沿用"挑战集 → 消融 → 晋升"，无实证收益不动当前 hybrid；中文分词 BM25 / RRF / reranker 由此进