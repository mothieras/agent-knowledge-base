# ROADMAP — 从只读检索到共享 Memory / Knowledge

> 版本：v1.3 阶段性路线图，2026-09-21。阶段 1 已全部交付（2026-09-14）；阶段 2 已全部交付（2026-09-21，条目服务核心 T1–T5 + 真实 Agent 验证 T6 + 文档派生条目 T7）。总体顺序已确认：先小步优化现有文档入库流程，再建设 Agent 自主记录与共享；阶段 3 为后续建设建议。具体任务、参数和排期在对应阶段细化。产品约束与待决策项见 [PRODUCT](PRODUCT.md)，完成状态与历史证据以 [README](README.md) 为准。

先选定有证据的具体问题及验收标准，再实施和验证。未完成项仍处于计划状态，文档交付不等于实现进度；建议的技术方案须在设计时验证，不能仅因写入本路线图就当作现成能力。

## 当前起点

当前已交付：受治理示例语料、hybrid 检索、证据回查、HTTP/MCP 接入与可选双模式问答，并有质量与规模评测证据；阶段 1 的文档入库质量优化（样例集、失败终态结构化、OCR 禁用双证据、原文件定位与分块边界）已全部交付；阶段 2 已全部交付（2026-09-21）——SQLite 持久条目、协作修订与幂等、生命周期与有效期、FTS5 + jieba 预分词的无模型全文搜索、长条目分块检索、HTTP/MCP 写接口，两个真实 Agent 客户端（Pi 与 Claude Code）验证与价值对照实验，文档派生条目与来源关系（文档注册表、`source.document` 引用、来源回查工具）。语料检索仍依赖 embedding 模型（hybrid 检索）；条目全文检索路径完全无模型。

离线入库链路（Markdown/TXT/PDF → 父子分块）与 embedded Qdrant 仍是“离线构建、在线只读”模式；条目库为独立 SQLite（WAL），文档注册表为独立 SQLite（离线 ingest 注册版本化文本），并发写入由阶段 2 交付，三者互不牵连。

剩余（阶段 3 候选）：在线/异步文档导入、完整备份恢复设计（D7）、检索版本/有效期过滤、可选向量/hybrid 条目检索与消融。

以下阶段的目标是逐步跑通使用流程；每一阶段都有可验证结果，不按技术组件是否存在判断完成。

## 各阶段开始前：准备规则与验收样例

只收敛即将实施阶段所需的契约。Memory/Knowledge 的删除、共享与在线写入细节，不作为优化现有离线文档流程的前置条件。

阶段 1 对应 PRODUCT 的 D1：冻结代表性文档、现有处理结果、查询/qrels 与质量门槛，选出一组能独立验收的优化项。以下阶段 1 清单是检查维度，具体结构性改进按实际问题选择，避免把“小步优化”扩成解析器重写。

阶段 2 按子步骤处理 D2–D6：先确定分类样例、项目与条目生命周期、搜索新鲜度、容量目标，以及稳定 ID、修订、来源、幂等和错误行为；文档来源关联子步骤再确定 D6。已确认的搜索范围、保留与有效期规则直接转为验收条件。D7 在承载唯一副本的真实资料前落实。

各阶段均需标记旧检索评测的可比范围，先定义行为，再选择满足行为的实现。

## 阶段 1：小步优化现有文档入库流程

> 状态：✅ 已全部交付（2026-09-14），验收证据见「阶段交付记录」。以下保留交付口径与验收条件供追溯。

目标：在已有“文档输入 → 规范化 → 父子分块 → 入库 → 检索与证据回查”的链路上，补齐质量与可靠性缺口，交付一个可独立验收的小改进。

交付建议：

- 先覆盖已有 Markdown、UTF-8 TXT、文本型 PDF 的真实样例，检查现有解析、命名和来源映射；常见格式扩展按实际需要加入，不将所有格式支持作为本步完成条件。
- 明确不支持格式、无可用文本、重复输入、解析失败和入库失败的结果，验证同名不同来源与重试行为，避免静默遗漏或错误跳过。
- 用样例定位标题、正文顺序、代码、表格和分块边界的问题，针对证据充分的缺口优化；保留现有父子检索与有界上下文回查能力。
- 验证来源、span、hash 与索引快照的一致性；记录解析/分块配置，规范化文本偏移与原文件定位分开表达。
- 明确关闭 OCR 并验证实际行为；解析步骤避免引入必需模型推理。现有 embedding 检索保持其已公开依赖，完整无模型搜索放在阶段 2 建设。

验收：

- 已有离线入口完成导入 → 分块入库 → 搜索 → 证据回查，代表性格式有真实文件证据；仍遵守 embedded Qdrant 同目录单进程持有、重建前停服务的约束。
- 覆盖正常文件、空文本/无文本层 PDF、重复输入、同名不同来源与失败重试，结果可解释，失败不能冒充成功；实际确认不触发 OCR。
- 检查文字与结构保留、分块边界和预算、来源定位；span/hash 与实际规范化文本一致，无法恢复的原文件定位明确缺失。
- 改解析、分块或语料时重跑规定的全量评测；新策略在冻结样例上对照现有策略，报告实际快照与配置，可比较路径遵守原回归纪律，收益不足不默认晋升。

本步按已有离线入口验收。通用在线上传、异步导入平台、协作写入和完整文档历史另行建设，不自动纳入这次小步优化；阶段 2 的未决规则不阻塞本步。

## 阶段 2：Agent 自主记录与共享

> 状态：✅ 已全部交付（2026-09-21）。条目服务核心 T1–T5（2026-09-17/18）与真实 Agent 验证 T6、文档派生条目 T7（2026-09-21，spec `docs/spec/PHASE2-T67.md`）全部完成，证据见「阶段交付记录」与 README；两客户端实测报告 `eval/reports/agents-2026-09-21.md`。

目标：调用方 Agent 能自主判断哪些信息值得记录，写入 Memory 或 Knowledge，自己或其他 Agent 在后续任务中找到并修订它；服务核心过程不依赖模型。自主决策属于调用方，服务提供工具与确定性契约。

本阶段与调用方原生记忆配合使用，验证不同 Agent 共同维护内容的收益。客户端通过 HTTP/MCP 工具接入，不依赖读取、改写或同步其内部记忆文件。

交付建议：

- 持久条目、类型、全局/单项目/多项目关联，稳定 ID、修订历史和作者记录。
- 按 ID 读取、全文搜索与过滤，明确写入和搜索可见的关系。
- 原子版本检查、冲突响应、幂等写入和已选定的生命周期操作。
- 默认持久保留、调用方指定有效期与到期过滤；到期后可显式回查原文和历史，不因长期未访问自动遗忘。
- HTTP/MCP 公共接口与访问凭据；所有获准接入的 Agent 共用全部内容。
- 提供调用方的工具说明与使用样例，明确何时记录到共享服务、何时检索或回查，以及新增/修订、范围选择、来源、冲突重读和重试规则；验证真实 Agent 在保留原生记忆时的使用流程。
- 沿用 L0–L4 分层约束，协议层通过 L2 公共接口访问能力。

验收：

- 真实外部 Agent 完成新增 → 搜索 → 回查 → 修订 → 历史回查；服务重启后数据保留。
- 在有记录依据的实际任务中，调用方 Agent 自主选择值得保存的内容、类型与范围并调用写工具，使用另一种客户端的 Agent 在后续任务中检索复用；同时覆盖用户明确要求记录的场景。至少覆盖两种真实 Agent 客户端，记录其版本、模型、原生记忆配置与初始状态；不通过复制原生记忆目录代替服务共享。服务端不承担自动语义提炼。
- 两类条目分别覆盖全局、单项目、多项目关联，按冻结规则验证过滤结果；多项目关联保留同一条目身份，第一版不因项目关联限制读写权限。
- 搜索未指定范围时只查全局；指定 A 时纳入全局及关联 A 的条目，不纳入只关联 B 的条目；指定 A、B 时按并集匹配，同一条目只返回一次；显式跨所有项目时纳入全部范围。分别验证默认双类型与单类型筛选，范围过滤先于最终结果截断。
- 全局条目无需补关联即可参与新项目搜索；服务不以项目内容自动覆盖全局内容，结果保留类型、范围、来源与修订信息供调用方判断。
- 两个客户端同时更新同一修订，只允许一个成功，另一个收到冲突；独立条目可以正常写入。
- 写入成功但响应丢失后的重试，返回原操作结果，不重复修改；同幂等标识的不同内容不会复用成功结果。
- 搜索命中与回查修订一致；生命周期操作与修改的竞争没有丢更新。
- 两类条目未设有效期时，即使长期未访问也不会自动消失；达到显式有效期后，默认搜索在所有项目范围模式下均不返回该条目，显式历史查询及回查仍能获得原文、修订和到期信息。
- 用可控时间验证到期前、到期时和到期后的行为；索引延迟或服务重启不会让已到期条目混入默认结果，旧有效期处理不会覆盖并发提交的新修订；访问记录不会自动续期。
- 禁用生成、embedding 和 rerank 模型后，仍完成核心流程，并达到阶段 2 开始前冻结的基础检索门槛。

不包含：多用户、私有权限、自动语义整理。并发正确性属于本阶段必要条件，不放到最后补做。

价值验证建议：在相同任务、初始资料、客户端配置与预算下，对照“原生记忆”与“原生记忆 + 本服务”。原生记忆的初始状态可重置或隔离，避免前一次运行的记录进入后一次对照；不限制对照组正常读取已提供的资料。按实际场景记录任务完成情况、用户重复转述或纠正次数、旧内容误用、耗时及总 token（包含调用方整理与取用内容的开销）。样例与门槛在对应子步骤实施前冻结，报告收益与失败，不预设接入服务必然更好；文档来源关联流程待下述子步骤交付后纳入。该验证不要求阶段 1 提前实现共享写入。

### 文档派生条目与来源关系

> 状态：✅ 已交付（2026-09-21，T7/D6，spec `docs/spec/PHASE2-T67.md` §4）。文档注册表（离线 ingest 注册版本化规范化文本，内容变化 version+1、旧版本永久保留）、`source.document` 引用（写入时机械校验存在与 span 合法）、`resolve_document`/`list_document_versions`/`read_document` 回查工具、长条目分块检索。文档更新 = 离线重新 ingest → 注册新版本；旧引用仍指向原修订，经两客户端端到端实测坐实。

## 阶段 3：检索增强与运行可靠性（后续建议）

目标：在可用核心之上，用实测决定增强能力，并验证长期保存是否可靠。

交付建议：可选向量/hybrid 检索的适配与消融；索引恢复和重建；备份恢复；容量及并发验证；必要的运行状态与故障诊断信息。版本/有效期过滤仍是待办：泄漏靶子 ch029（`eval/challenge_set.jsonl`）与 version_conflict 类题已备，相关改动纳入回归；version_conflict 按设计需返回两版，过滤不得误伤。

验收：

- 基础检索和增强检索分别报告同一冻结数据集上的质量、延迟与资源开销；无收益不默认启用增强。
- 模型不可用时基础流程仍正常运行，所用检索模式及降级状态明确可见。
- 备份恢复后正文、修订和来源完整；索引重建不会丢失原始记录或改变历史证据含义。
- 在阶段 2 前约定的容量与并发负载下满足门槛；不以旧演示版的规模结果替代新写入路径验收。

条目持久化与重启恢复已是阶段 2 必需项；若某个版本要承载唯一副本的真实资料，备份恢复验收须提前为使用前置条件。

## 后续候选，不承诺实施

多用户与私有分区、精细权限、语义自动整理与按使用情况自动遗忘、任务交接和实时共享状态。只有出现具体场景并确认产品范围后，才进入阶段计划；调用方指定的确定性到期处理已在阶段 2 内。

## 全程验证纪律

- 保留原 30 题与原检索计分范围；新增 Memory/Knowledge 用例独立报告，不混用分母。
- 质量集 0 ERROR / 0 SKIP，真实索引验收不能靠 skip 替代；报告绑定实际代码、数据、模型和参数。
- 行为性改动运行全量 pytest（纯文档、注释等低风险改动观察真实输出即可）；改检索、Agent 或语料时重跑全量评测，先冻结挑战集、qrels 与门槛再消融；服务变更做 readiness 检查，MCP 变更做协议与真实客户端验证。
- 更新 README 时只写已实现且有证据的行为；文档检查不能当作功能验收。
- 进度不足时先砍附加算法、界面美化与额外客户端适配；评测、证据正确性与错误行为最后砍。

## 阶段交付记录

每阶段收尾记录实际完成范围、代码与数据版本、验证命令和报告位置、已知限制及下一阶段需要处理的事项。完成状态更新到 README，PRODUCT 的待决策项按结果收敛；出现范围变化时同步修改本路线图，不回写或掩盖历史评测结果。

### 阶段 1（2026-09-13 → 09-14）：小步优化现有文档入库

- **范围**：T0 环境与基线（A/B/C）→ T1 样例集 + 两级验证 harness → T2 OCR 禁用双证据 → T3 入库失败终态结构化 → T4 原文件定位显式 missing → T5 分块边界证据。全部完成，无范围变化。
- **代码**：`9a0e389`（MPS embedding 串行锁）、`8affb2a`（样例集 + harness）、`2650477`（S6 双证据）、`c428ce1`（终态结构化）、`bfe7584`（index_id 确定性口径）、`3d15ae0`（origin_page missing）、`44eecd1`（边界证据）。
- **验证**：pytest 119 passed 0 skipped；挑战集回归锚点 Recall@5=1.000 持平（`eval/reports/challenge-2026-09-14.*`）；Level B 15 步终态全中目标表、正样 7 源；主语料 18 篇两次 scratch 重建 index_id 一致（`sha256:5c18f9f2…473f03`），且逐点 payload 与 09-13 基线完全一致（354 child 零差异）。
- **已知限制 / 移交**：页界切段与跨页表格重复表头属 pymupdf4llm normalizer 行为（阶段边界明确不重写解析器）；markdown 输出双扩展名（slug 含扩展名再拼 `.md`，无害）；OCR 禁用为依赖门控而非参数门控，升级依赖须重验；PDF 原文件页码映射未实现（显式 missing，实现需 chunker 核心重写）。
- **下一阶段**：阶段 2 条目接口设计（D2–D6 待决策项收敛）。

### 阶段 2（2026-09-16 → 09-21，✅ 全部交付）：Agent 自主记录与共享

- **范围**：PHASE2 冻结 spec（2026-09-16）→ T1 SQLite 条目存储与契约服务 → T2 HTTP/MCP 接口与工具说明 → T3 无模型全文搜索（jieba 预分词实测采定 + 挑战集冻结）→ T4 生命周期与到期端到端 → T5 服务级验证（最小备份恢复冒烟 + 容量/并发/重启实测）→ PHASE2-T67 冻结 spec（2026-09-20）→ T6 真实 Agent 两客户端验证（Pi 0.86.0 + Claude Code 2.1.268，五场景 + 价值对照两臂）→ T7 文档派生条目与来源关系（D6）。无范围变化。
- **代码**：`5e48c2f`（D2–D5 设计收敛）、`67a7314`（T1 条目存储与契约服务）、`5ae5c9c`（T2 接口与工具说明）、`d72a447` + `16f4b90`（T3 挑战集冻结 + FTS5 搜索）、`d43ef85`（T4 生命周期与到期）、`316e826` + `2ae2453`（T5 备份冒烟与容量实测）；T6/T7 变更见收尾提交（文档注册表、source.document、来源回查工具、长条目分块、Agent 验证材料）。
- **验证**：pytest 249 passed 0 skipped；条目检索挑战集三门槛全过（recall@5=mrr@10=1.0，契约 sha256 冻结，`eval/reports/entries-challenge-2026-09-18.md`，分块检索改动后回归 `entries-challenge-2026-09-21.md`）；容量/并发/重启实测达标（1 万 active 条目 / 10 万修订、3 并发写 0 busy、搜索 P95 7.7ms 对 500ms 门槛、SIGTERM 重启数据保留、禁生成模型核心流程全链，`eval/reports/entries-capacity-2026-09-18.md`）；生产 ingest 双跑 index_id 与冻结基线一致（注册钩子不改快照输入）；两客户端实测与价值对照报告 `eval/reports/agents-2026-09-21.md`。
- **已知限制 / 移交**：author 为自报字符串、非强身份（与部署级单 Bearer 凭据不绑定，T6 实测可见：同一次验证中 modifier 出现 `claude-code` 与 `claude/2.1.268` 两种自报）；v1 无私有分区与多用户；在线/异步文档导入移交阶段 3；D7 完整备份恢复设计移交阶段 3（T5 仅最小冒烟，docs.db 纳入备份范围时一并设计）；来源引用只机械校验 span 合法，不校验条目正文主张都在 span 内（T7b 实测发现一处引用越界，语义校验属调用方责任）。
- **下一阶段**：阶段 3 检索增强与运行可靠性（版本/有效期过滤、备份恢复、在线导入范围）。
