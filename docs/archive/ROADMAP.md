# 路线计划：五天检索优先演示版

> 状态：计划，未开始按本路线验收。用户已选择 **A：五天检索优先演示版**，不是全量共享知识服务五天交付。
> 已完成能力以 [README](README.md) 为准；范围以 [PRD](docs/PRD.md) 为准；实现边界见 [DESIGN](docs/DESIGN.md)。旧路线保存在 [历史归档](docs/archive/roadmap-before-retrieval-demo.md)，不再作为当前执行要求。

## 1. 本次交付与不交付

目标闭环：

```text
离线导入受控资料
  → 不配置生成模型也能启动、检索、回查证据
  → HTTP / MCP 共用契约，Pi 实际调用
  → 同一问题比较普通 RAG 单图与 Agentic RAG 双图
  → 冻结评测、记录质量/延迟/成本
  → Python / Docker / Gradio 可复现演示
```

五天版只有在线只读的固定资料快照。可以让多个调用者使用，但所有获准访问者看到同一资料，不宣传私有分区或协作平台。

以下明确顺延：每 Agent 凭据与公私隔离、共享编辑/删除、文件上传和异步入库 API、乐观修订与文档历史、在线一致性发布及防删除复活。完整需求仍保留为 PRD 的 F-01–F-09。

不做 Web 工具、聊天历史/HITL 恢复、PostgresSaver、管理后台、OCR、DOCX 或通用记忆学习。既有代码中的 history 能力属于待迁移现状，不是新版本继续承诺的目标。

## 2. 起点和已知债务

规划时工作树 HEAD 为 `c6eb423`，不是未来基线绑定值。每次真实报告必须记录执行时的实际 commit/dirty 状态。

可复用：M1 `Retriever`/`RetrievalHit`、父子块/精确 span、当前 Qdrant hybrid、双图、HTTP/SSE/client/Gradio、30 题及历史报告。

尚待实现：生成运行时解耦、纯检索服务接口、普通 RAG 单图、MCP、单次协议和显式结果、API citation/回查、离线 TXT/文本输入、Docker 交付。当前 `api/events.py` 仍依赖 L0 类型与节点细节，不能称其已完成新分层。

历史基准见 [2026-09-01 报告](eval/reports/baseline-2026-09-01.md)：30 条、0 ERROR/0 SKIP，20 条进入检索计分。Recall@5 1.000、MRR 0.975 是这套固定题目的结果，不是通用上限，也不是新版本验收结果。

## 3. 五天执行计划

Day 1–5 从开始实施计，不绑定未经确认的日历日期。每天的输出是检查点，不保证所有任务恰好耗时一天；依赖和门禁优先于日期。

| 日 | 工作重点 | 必须留下的证据 | 检查点失败时 |
|---|---|---|---|
| **Day 1** | 固定范围与评测契约；保存原始基线/trace；扩展并冻结检索题及 qrels；确定单次 decision 标注；Pi/MCP ASGI 小型兼容性验证；检查 ARM64 Docker 依赖 | 原 baseline 运行及 bindings；冻结题集/计分/配置；Pi transport/Bearer/structured result 记录；镜像依赖可行性结果 | 不先改召回/排序或用新答案反推题目；兼容性失败先定位，不拖到收尾 |
| **Day 2** | 解耦检索与可选生成 bootstrap；L2 公共接口/证据 DTO；HTTP search/read_evidence；MCP 只读工具；离线文本/MD/TXT/PDF 快照与校验 | 无生成模型入库→ready→HTTP/MCP→回查；同查询有序证据等价；无效 ID/快照/凭据测试 | 保持单进程、在线只读，不引入写任务或 Qdrant 集群扩大关键路径 |
| **Day 3** | 固定 RAG 单图；现有双图单次化；共享预算；decision/citation 校验；SSE/client/Gradio 迁移；可选 MCP ask | 三种 decision 和错误路径；引用精确回查；跨请求不串状态；所有协议终态一致；全量测试/eval | 不靠仅改 prompt 冒充显式决策；不保留无鉴权 history 后门；不删 badcase |
| **Day 4** | 按冻结契约跑直接检索、单图与双图比较；审计引用/拒答/澄清 badcase；规模快照与 3 并发；完成 Docker 原生流程 | 全题 per-item 结果、质量/成本/延迟/资源报告；同配置对照；原生与容器 smoke 初稿 | 停止额外算法和 UI 实验；先处理回归，无法证明的改进不晋升 |
| **Day 5** | 空数据复现；Pi 真实端到端；Python/Docker/Gradio 演示；文档和发布清单收尾 | requirements/模型/语料/索引 bindings，测试/eval/health/Pi/容量证据，演示步骤和已知限制 | 如实列未完成项，不通过改门槛、隐藏失败或修改旧报告宣布完成 |

不要求五天内完成 jieba sparse、手写 RRF、reranker 或完整算法消融。优先比较同一检索器下普通 RAG 与 Agent 编排的差异；负结果是有效成果。

## 4. 评测契约：先冻结，后改变行为

### 4.1 三组数据各司其职

1. **原 30 题回归资产**：保留题目、来源、旧报告和旧定义；规划中的新执行器不能回写历史结果。原 20 条检索计分范围保持可追踪。
2. **检索挑战集**：至少 40 条实际进入直接检索计分的问题，可包含原有检索题；覆盖术语、改写、hard negative、多跳、来源版本冲突。补 qrels，标注等级与计分粒度。
3. **功能/负载 fixture**：记忆/反馈文本、伪造 ID、版本冲突、快照失效、文件失败、并发等。规模扩增数据不混入质量评测，未来私有权限 fixture 不当成本轮能力验收。

Day 1 在新行为实现前冻结题集 hash、qrels、decision 标签、参数和数值门禁，建议产物为 `eval/acceptance.yaml` 与 per-item bindings。文件在本次文档工作中尚未创建，不把计划中的尺子写成现有资产。

普通 RAG 与双图比较时固定语料、index_id、模型、retriever、k、评分模型与价格口径。分别记录各模式 prompt 与预算，不要求两种流程有相同调用次数，也不隐瞒其差异。

### 4.2 计分与回归

- 检索：Recall@5/@7、MRR、source precision@5、nDCG@5、各题型明细、来源/content hash 的有序 trace。明确 source 去重、分级相关性与未标注候选的处理规则。
- 生成：faithfulness、answer relevancy、context precision/recall、decision、引用有效性和证据支持性。引用有效性是机械校验，不能代替语义支持性。Day 1 另冻结至少 10 题的语义审计子集，包含可答、部分可答和冲突材料，并预标关键事实、支持证据和允许的缺口；两种问答模式都执行。
- 体验：直接检索、单图、双图分别记录 P50/P95、错误、工具调用、token、生成/judge 成本与峰值资源。SSE 首次状态事件不冒充模型首 token。
- 当前 retriever 不变时，新应用/协议适配不能改变有序命中 ID、来源或内容；原 20 条直接检索回归应保持 Recall/MRR，不以生成轨迹漂移解释检索适配回归。
- 原本包含 HITL 意义的 `ambiguous_followup` 在新产品中评估单次 `clarification_required`，保留问题 ID 和新旧定义映射。旧暂停率与新单次澄清率并列报告，不直接宣称同比提升。
- 历史 runner 的 `refusal.precision = 1 - false_refusals / answerable_n` 不是标准 precision，其中 `answerable_n` 是已产生 answer 字段、类别非 unanswerable 的题数，含澄清题。历史文件原样保留，新汇总显式标为 `legacy_specificity`（历史非误拒率），不拿它与新 precision 同比。
- 新 decision 集以“应拒答”为正类：TP=应拒且实际 refused，FP=非应拒却 refused，FN=应拒但返回其他成功 decision，TN=非应拒且未 refused；precision=TP/(TP+FP)，recall=TP/(TP+FN)。误拒率=明确可答题中 refused 数/明确可答题数，过度澄清率=明确可答题中 clarification_required 数/明确可答题数。分母为零时标 undefined，不能当作通过。错误/未执行单列并阻断通过，不塞进 TN，也不从总题数消失。
- 新单图与新双图分别建立自己的首轮基线；不能要求普通 RAG 的轨迹等于旧双图，也不能因此删除原有题目。新双图的可比回答项仍要与旧基线比较。
- 当指标波动时，使用相同模型配置的预先约定复验/重评分解释；不看到结果后修改题目、qrels、阈值或挑选最好一次。

### 4.3 硬门禁与工程默认验收线

所有数值都是目标，不是当前通过声明。Day 1 将下列口径机器化；发现分母/指标定义无法计算时，必须在新行为实现前记录修订原因。

- 冻结质量集完整执行，**0 ERROR、0 SKIP**；错误和未执行项保留在报告及完整分母中。
- HTTP/MCP 直接检索等价性、引用 ID/span/hash 机械有效性、快照不误解析：**100%**。协议等价比较共同应用预算裁剪后的完整响应，检索质量另计未裁剪 artifact。
- 冻结语义审计集：把每个 `answered` 结果拆成全部可核验知识性声明，逐条核查所引证据是否支持；未引用的知识性声明也进入分母。支持率须 **100%**，且满足预标关键事实和预期 decision，不能靠空答案/全部拒答把分母变成零。固定 judge 可辅助拆分/初判，由维护者复核支持关系与争议并保存标注。真实但无关的引用算不支持；未复核、流程未冻结或任一模式不达标，D-06 不通过。此门槛只针对冻结审计样本，不是所有开放问题的事实正确性保证。
- 真正无命中、检索故障、LLM 未配置、上游失败在测试中 **100% 区分**；没有证据不得返回伪造 citation。
- 冻结 decision 集：拒答 recall/precision 各 **≥0.90**；明确可答题误拒率和过度澄清率各 **≤5%**。小题集离散效应必须列出原始计数；不虚构统计精度。
- 单次澄清问题是否满足预期依据冻结标签计分，不新增“澄清后同 thread 解决率”门禁。
- 当前检索器及固定原语料不变时，检索回归指标和有序 trace 不下降/不变；任何有意检索策略变更需另列消融和晋升门禁。
- 生成质量以冻结的同配置基线与预设重复评分程序判定，不使用旧路线中不再对应当前报告的孤立阈值。Day 1 必须写明比较样本、重复次数及可接受差值，未冻结则不能声称生成质量验收通过。
- 容量测试实际构建约 100 文档/1 万块，3 并发执行至少 100 个有效只读查询；有效请求无错误、结果不串线，所有引用可回查。记录排队与资源，不只报告成功请求延迟。
- 超时/限额按技术设计显式失败；本轮不承诺未测出的绝对检索延迟 SLA。容量或平台测试未做完，标记未验证，不拿小语料 smoke 替代。

纯文档修改不需要付费 eval；真正修改检索、Agent 或语料后，仍必须按项目规则重跑全量评测。语义迁移需要单独报告，不是允许忽略回归的理由。

## 5. 验证与发布证据

每次实施改动：

```bash
.venv/bin/python -m pytest tests/
```

检索/Agent/语料改动，在停止争用同一 embedded 索引的服务后：

```bash
cd eval
env -u ALL_PROXY -u all_proxy ../.venv/bin/python run_eval.py
```

上面是现有 runner 入口；新模式/挑战集 runner 的命令在实现后补充，不能提前宣称已有相应 CLI 参数。

服务变更至少验证原生入口：

```bash
cd src
env -u ALL_PROXY -u all_proxy ../.venv/bin/uvicorn api.main:app --host 127.0.0.1 --port 8000
# 另一个终端
curl http://127.0.0.1:8000/health
```

release evidence 清单：

- 实际代码 commit/dirty 状态、依赖版本、语料 revision/hash、index_id、分块/模型/prompt 配置。
- pytest 完整摘要、真实索引结果、HTTP/MCP/SSE 契约结果。
- 原始与扩展题集报告、单图/双图对照、badcase、模型/judge 用量及费用口径。
- Pi/adapter/SDK/协议版本，认证与实际工具调用记录；脱敏，不保存密钥。
- 原生 Mac 与 Docker 的资源配置、空数据准备与 smoke；模型下载时间和暖启动分开。
- 规模/并发数据、错误与超时、峰值资源。
- 清楚标注未交付项，README 只把有证据的功能从目标移到已完成。

用户已授权沿用现有模型进行有界评测，无须逐轮确认费用；仍需避免无界工具循环、完整评测无限重跑、日志泄密和未经授权采购。

## 6. 五天之后

### P1：共享知识层

以 PRD F-01–F-09 为范围单独排期。身份/权限、文档目录、编辑修订、异步任务、发布可见性、删除 tombstone、历史回查需要联动设计与测试；不能先开放写接口再补授权。

此时评估事务型 catalog、Qdrant server 和后台任务机制。先验证权限过滤、搜索→父文档回查、写入竞态与故障恢复，再声明共享知识服务可用。没有多租户平台、用户注册或 OIDC 的隐含承诺。

### P2：检索实证升级

继续原路线中仍适用的“挑战集 → 消融 → 晋升”原则，比较 dense、现有 hybrid、中文 sparse、融合与可选 reranker。质量、困难分组、P95 和资源阈值必须在候选实现前冻结；未达标时保留基线并报告负结果。

来源有效期过滤属于独立 correctness 工作，不等同于文档编辑修订。五天版不因已有 metadata 就声称它已实现。

### 不再延续的旧目标

跨请求会话、真实两轮 HITL 恢复、PostgresSaver 不再是当前产品路线。将来若重新需要，必须重新确认需求，而不是从归档路线中自动复活。

## 7. 范围变更原则

最先停止的是附加算法、美化和额外客户端，不是评测、证据正确性或清晰的错误行为。

PRD 的 D 系列关键项若无法在五天内通过，报告部分完成并明确阻塞；不能自行把未完成改成可选。F 系列恢复排期或新增联网/多轮功能须重新确认。
